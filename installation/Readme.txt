Installation:

1. Running SQL scripts for creating database tables (createTableAdmins.sql, createTableEvents.sql),
2. Setting up parameters for database connection in ./../dat/config.php file,
3. logging to the service using root account.

